/* AUTO-GENERATED FILE. DO NOT EDIT MANUALLY.
 *
 * Source: docs/modules/*.md (converted into TS string constants).
 * Regenerate: python frontend/scripts/generate_help_docs.py
 */

export type HelpDocKey =
  | 'cooling'
  | 'heating'
  | 'wind_cloud'
  | 'solar_efficiency'
  | 'emissivity_solar_cloud'
  | 'power_components'
  | 'angular_power'
  | 'in_situ_era5'
  | 'material_comparison'

export const HELP_DOCS_RAW: Record<HelpDocKey, string> = {
  "cooling": "## 模块：辐射制冷（Cooling）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"新建任务\" → 任务类型为 `cooling`（辐射制冷）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n在使用本模块前，用户需要：\n\n1. **上传反射率文件**：访问\"上传\"页面，上传 `reflectance.txt`（可见光波段反射率数据），透明材料可以额外上传透过率文档，系统会将透过近似为反射。\n2. **上传发射率文件**：上传 `emissivity.txt`（大气窗口8-13 μm发射率数据）\n3. **确认激活状态**：上传后文件会自动处理并设置为\"激活\"状态（系统会使用激活的材料进行计算）\n\n> **注意**：系统会自动使用AM1.5光谱（`default/AM1.5.xlsx`）、波长网格（`default/Wavelength.csv`）和配置（`default/config.ini`），用户无需手动选择。\n\n#### 2.2 任务参数（WebUI表单）\n\n创建任务时可在Web界面设置：\n\n- **精度等级**（Precision）：`low`（1000角度步）、`medium`（2000步，默认）、`high`（5000步）\n- **大气预设**（Atmospheric Preset）：选择大气透过率模型文件（如 `clear_sky.dll`）\n- **自然对流**（Natural Convection）：是否启用自然对流计算（默认启用）\n- **相变参数**（可选）：\n  - 相变起始温度（°C）\n  - 相变功率（W/m²）\n  - 相变半宽度（°C）\n\n#### 2.3 输出结果\n\n任务完成后，在任务详情页可查看：\n\n- **图表**：$p_{net}$ 随 $T_{film}$ 变化的曲线图（多条曲线对应不同的 $h_c$ 值）\n- **摘要数据**（Summary）：\n  - `Power_0`：当 $T_{film}=T_a$（$\\Delta T=0$）时的净制冷功率 $p_{net}$（W/m²）\n  - `avg_emissivity`：材料普朗克加权平均发射率\n  - `R_sol`：太阳加权反射率（AM1.5加权）\n- **数据下载**：可下载CSV格式的完整计算结果\n\n### 3. 采用的核心物理量与公式\n\n#### 3.1 太阳加权反射率与太阳吸收\n\n- 太阳加权反射率（AM1.5）：\n\n$$\nR_{sol}=\\frac{\\int R(\\lambda)S(\\lambda)\\,d\\lambda}{\\int S(\\lambda)\\,d\\lambda}\n$$\n\n- 太阳吸收率：\n\n$$\n\\alpha_s = 1-R_{sol}\n$$\n\n- 太阳吸收功率：\n\n$$\nQ_{solar}=\\alpha_s S_{solar}\n$$\n\n#### 3.2 半球角积分权重\n\n本模块将天空半球（天顶角 $\\theta\\in[0,\\pi/2)$）离散，使用权重：\n\n$$\nd\\Omega = 2\\pi\\sin\\theta\\cos\\theta\\,d\\theta\n$$\n\n#### 3.3 材料向外辐射功率 $p_r$\n\n用普朗克谱（数值稳定实现）与材料光谱发射率 $\\varepsilon_s(\\lambda)$ 在大气窗口波段积分：\n\n$$\np_r=\\int_{\\Omega}\\int_{\\lambda}\\varepsilon_s(\\lambda)\\,I_{BB}(\\lambda,T_s)\\,d\\lambda\\,d\\Omega\n$$\n\n#### 3.4 大气向下辐射功率 $p_a$\n\n大气透过率文件提供 zenith 方向透过率 $\\tau(\\lambda)$。对任意天顶角，采用路径长度修正：\n\n$$\n\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}\n$$\n\n从而：\n\n$$\np_a=\\int_{\\Omega}\\int_{\\lambda}\\varepsilon_s(\\lambda)\\,\\varepsilon_{atm}(\\lambda,\\theta)\\,I_{BB}(\\lambda,T_a)\\,d\\lambda\\,d\\Omega\n$$\n\n#### 3.5 对流/换热项 $Q_{conv}$\n\n模块支持两种方式：\n\n- **自然对流**：由 `core/physics.py::calculate_convection_coefficient()`（Churchill-Usagi混合）给出 $h_{nat}$\n- **额外换热系数扫描**：配置中的 `HC_VALUES`（记为 $h_{extra}$）\n\n##### 3.5.1 自然对流换热系数 $h_{nat}$\n\n自然对流采用 Churchill-Usagi 混合法，结合自然对流和强制对流（虽然制冷模块中通常风速为0，但函数实现了完整的混合框架）。\n\n**物性参数计算**（基于膜温 $T_{film}=T_a+\\Delta T/2$）：\n\n- 空气密度：$\\rho = 1.225 \\times (273.15 / T_{film})$ kg/m³\n- 动力粘度：$\\mu = 1.81 \\times 10^{-5} \\times (T_{film} / 273.15)^{0.7}$ Pa·s\n- 热导率：$k_{air} = 0.024 \\times (T_{film} / 273.15)^{0.8}$ W/(m·K)\n- 比热容：$c_p = 1005$ J/(kg·K)\n- 运动粘度：$\\nu = \\mu / \\rho$ m²/s\n- 热扩散率：$\\alpha = k_{air} / (\\rho c_p)$ m²/s\n- 普朗特数：$\\Pr = \\nu / \\alpha$（限制在 0.68–0.75 范围内）\n- 体积膨胀系数：$\\beta = 1 / T_{film}$ K⁻¹\n\n**自然对流部分**（当 $|\\Delta T| > 10^{-3}$ K 时）：\n\n瑞利数：\n\n$$\nRa = \\frac{g \\beta |\\Delta T| L_{char}^3}{\\nu \\alpha}\n$$\n\n其中 $g=9.81$ m/s² 为重力加速度，$L_{char}=1.0$ m 为特征长度。\n\n努塞尔数（根据 $Ra$ 范围选择）：\n\n$$\nNu_{nat} = \\begin{cases}\n0.54 Ra^{0.25} & \\text{if } Ra < 10^7 \\\\\n0.15 Ra^{1/3} & \\text{if } Ra \\geq 10^7\n\\end{cases}\n$$\n\n自然对流换热系数：\n\n$$\nh_{nat} = \\frac{Nu_{nat} \\cdot k_{air}}{L_{char}}\n$$\n\n**强制对流部分**（当风速 $v > 10^{-3}$ m/s 时，制冷模块中通常为0）：\n\n雷诺数：\n\n$$\nRe = \\frac{v L_{char}}{\\nu}\n$$\n\n努塞尔数（根据 $Re$ 范围选择）：\n\n$$\nNu_{forced} = \\begin{cases}\n0.664 Re^{0.5} \\Pr^{1/3} & \\text{if } Re < 5 \\times 10^5 \\\\\n0.037 Re^{0.8} \\Pr^{1/3} & \\text{if } Re \\geq 5 \\times 10^5\n\\end{cases}\n$$\n\n强制对流换热系数：\n\n$$\nh_{forced} = \\frac{Nu_{forced} \\cdot k_{air}}{L_{char}}\n$$\n\n**Churchill-Usagi 混合**（幂次 $n=3$）：\n\n$$\nh_{conv,mixed} = \\left(h_{natural}^3 + h_{forced}^3\\right)^{1/3}\n$$\n\n在制冷模块中，由于风速通常为0，$h_{forced} \\approx 0$，因此 $h_{conv,mixed} \\approx h_{natural}$。\n\n最终自然对流换热系数：\n\n$$\nh_{nat} = \\max(1.0, h_{conv,mixed}) \\quad \\text{[W/(m²·K)]}\n$$\n\n##### 3.5.2 额外换热系数 $h_{extra}$\n\n额外换热系数 $h_{extra}$ 从配置文件 `config.ini` 的 `HC_VALUES` 中读取，是一个列表（例如：`[0, 5, 10, 15]` W·m⁻²·K⁻¹）。该系数用于模拟额外的导热/对流损失，例如：\n\n- 材料与基底的接触热阻\n- 额外的对流损失\n- 其他非辐射传热机制\n\n##### 3.5.3 总换热系数与对流功率\n\n总换热系数：\n\n$$\nh_{total} = h_{nat} + h_{extra}\n$$\n\n对流/换热功率（使用项目既有符号约定，以 °C 的温差计算）：\n\n$$\nQ_{conv} = h_{total} \\times (T_a - T_{film})\n$$\n\n其中 $T_a$ 和 $T_{film}$ 的单位为 °C。\n\n#### 3.6 净制冷功率（最重要输出）\n\n$$\np_{net}=p_r-p_a-Q_{conv}-Q_{solar}+P_{phase}\n$$\n\n其中 $P_{phase}$ 为可选相变附加功率（在高于相变温度后爬坡到平台）。\n\n### 4. 计算步骤\n\n1. 读取反射率 $R(\\lambda)$ 与 AM1.5 光谱 $S(\\lambda)$，插值到同一波长网格，得到 $R_{sol}$ 与 $\\alpha_s$。\n2. 读取材料发射率谱 $\\varepsilon_s(\\lambda)$ 与大气透过率 $\\tau(\\lambda)$，并插值到统一网格。\n3. 对每个膜温 $T_{film}$ 与每个换热系数场景 $h_{extra}$：\n   - 计算半球角积分（$\\theta$ 离散）下的 $p_r(T_s)$ 与 $p_a(T_a)$\n   - 计算 $Q_{solar}=\\alpha_s S_{solar}$\n   - 计算 $Q_{conv}=h_{total}(T_a-T_{film})$\n   - 合成得到 $p_{net}$\n4. 输出 $p_{net}(T_{film},h_c)$ 曲线/表格；给出 $\\Delta T=0$ 时的 $p_{net}$（即 `Power_0`）。\n\n### 5. 小案例（可复现）\n\n**目的**：计算材料在 $T_a=25^\\circ C$、$S_{solar}=1000\\text{ W/m}^2$ 下的净制冷功率 $p_{net}(T_{film})$。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件（`reflectance.txt`）：包含可见光波段的反射率数据\n   - 上传发射率文件（`emissivity.txt`）：包含大气窗口8-13 μm的发射率数据\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **创建任务**：\n\n   - 访问\"新建任务\"页面\n   - 任务类型自动设置为\"辐射制冷\"（cooling）\n   - 选择精度等级（建议：`medium`）\n   - 选择大气预设（如 `clear_sky.dll`）\n   - 确认自然对流选项（默认启用）\n   - 点击\"提交任务\"\n3. **查看结果**：\n\n   - 任务提交后自动跳转到任务详情页\n   - 等待任务完成（状态从 `queued` → `started` → `succeeded`）\n   - 在详情页查看：\n     - **交互式图表**：$p_{net}$ 随 $T_{film}$ 变化的曲线（Plotly图表，可缩放、悬停查看数值）\n     - **摘要信息**：`Power_0`、`avg_emissivity`、`R_sol` 等关键参数\n     - **下载数据**：点击\"导出CSV\"按钮下载完整计算结果\n\n**结果解读**：\n\n- **$p_{net}>0$**：表示净制冷功率，材料能够主动制冷\n- **$p_{net}<0$**：表示净制热功率，材料被加热\n- **$p_{net}=0$ 的交点**：对应的 $T_{film}$ 可视作该工况下的平衡温度（或平衡温差）\n- **不同曲线**：对应不同的对流换热系数 $h_c$，用于评估材料在不同换热条件下的性能\n\n### 6. 论文写法示例（可直接改数值）\n\n> We evaluated the radiative-cooling performance using an energy-balance model that combines hemispherical long-wave radiation, atmospheric back-radiation, convection, and absorbed solar power. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. The solar absorptance was obtained from the AM1.5-weighted reflectance $R_{sol}=\\int R(\\lambda)S(\\lambda)d\\lambda/\\int S(\\lambda)d\\lambda$, with $\\alpha_s=1-R_{sol}$. The net cooling power density was computed as $p_{net}=p_r-p_a-h_{tot}(T_a-T_s)-\\alpha_sS_{solar}$, where $p_r$ and $p_a$ were calculated by integrating Planck's law over the atmospheric window (8-13 μm) and the hemisphere, using $\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}$ to account for the zenith-angle dependence of atmospheric emissivity. The calculations were performed with angle discretization of 2000 steps (medium precision) and natural convection enabled. Reported curves show $p_{net}$ versus film temperature under ambient conditions of $T_a=25^\\circ$C and $S_{solar}=1000$ W m$^{-2}$.\n",
  "heating": "## 模块：辐射制热（Heating / Photothermal）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"新建任务\" → 选择任务类型为 `heating`（辐射制热）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n与制冷模块相同，用户需要先上传材料文件\n\n> **注意**：系统会自动使用AM1.5光谱、波长网格和配置，用户无需手动选择。\n\n#### 2.2 任务参数（WebUI表单）\n\n创建任务时可在Web界面设置：\n\n- **精度等级**（Precision）：`low`（1000角度步）、`medium`（2000步，默认）、`high`（5000步）\n- **大气预设**（Atmospheric Preset）：选择大气透过率模型文件（如 `clear_sky.dll`）\n- **自然对流**（Natural Convection）：是否启用自然对流计算（默认启用）\n- **相变参数**（可选）：相变起始温度、功率、半宽度\n\n#### 2.3 输出结果\n\n任务完成后，在任务详情页可查看：\n\n- **图表**：$p_{heat}$ 随 $T_{film}$ 变化的曲线图（多条曲线对应不同的 $h_c$ 值）\n- **摘要数据**（Summary）：\n  - `Power_0`：当 $T_{film}=T_a$（$\\Delta T=0$）时的净制热功率 $p_{heat}$（W/m²）\n  - `avg_emissivity`：材料普朗克加权平均发射率\n  - `R_sol`：太阳加权反射率（AM1.5加权）\n- **数据下载**：可下载CSV格式的完整计算结果\n\n### 3. 核心公式与物理意义\n\n制热模块与制冷模块共享 $p_r$、$p_a$、$Q_{solar}$、$Q_{conv}$ 的计算方式（详见制冷模块文档），区别在于“净功率的符号定义”：\n\n$$\np_{heat}=Q_{solar}+p_a+Q_{conv}-p_r-P_{phase}\n$$\n\n- 当 $p_{heat}>0$：材料在该工况下**净增热**（可视作净制热能力）\n- 当 $p_{heat}<0$：材料反而表现为净散热（趋向制冷）\n\n其中：\n\n- $Q_{solar}=\\alpha_s S_{solar}$，$\\alpha_s=1-R_{sol}$\n- $Q_{conv}=h_{total}(T_a-T_{film})$\n- $p_r$：材料向外长波辐射（取决于 $\\varepsilon_s(\\lambda)$、$T_{film}$）\n- $p_a$：大气向下长波辐射（取决于 $\\tau(\\lambda)$、$T_a$、角度积分）\n\n### 4. 计算步骤\n\n1. 由 $R(\\lambda)$ 与 AM1.5 光谱 $S(\\lambda)$ 得到 $R_{sol}$、$\\alpha_s$。\n2. 由 $\\varepsilon_s(\\lambda)$ 与 $\\tau(\\lambda)$ 计算半球积分的 $p_r$、$p_a$。\n3. 组合对流/换热项 $Q_{conv}$，并合成 $p_{heat}$。\n4. 输出 $p_{heat}(T_{film})$ 曲线，用于评估光热增温能力与换热敏感性。\n\n### 5. 小案例（可复现）\n\n**目的**：计算材料在不同 $h_c$ 下的净制热功率 $p_{heat}(T_{film})$，并读取 $\\Delta T=0$ 点的净制热功率。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**（与制冷模块相同）：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **创建任务**：\n\n   - 访问\"新建任务\"页面（`/jobs/new?type=heating`）\n   - 任务类型自动设置为\"辐射制热\"（heating）\n   - 选择精度等级（建议：`medium`）\n   - 选择大气预设（如 `clear_sky.dll`）\n   - 确认自然对流选项（默认启用）\n   - 点击\"提交任务\"\n3. **查看结果**：\n\n   - 任务提交后自动跳转到任务详情页\n   - 等待任务完成（状态从 `queued` → `started` → `succeeded`）\n   - 在详情页查看：\n     - **交互式图表**：$p_{heat}$ 随 $T_{film}$ 变化的曲线（Plotly图表）\n     - **摘要信息**：`Power_0`、`avg_emissivity`、`R_sol` 等关键参数\n     - **下载数据**：点击\"导出CSV\"按钮下载完整计算结果\n\n**结果解读**：\n\n- **$p_{heat}>0$**：表示净制热功率，材料能够主动增温\n- **$p_{heat}<0$**：表示净散热功率，材料趋向制冷\n- **材料特性影响**：\n  - 若材料具有高太阳吸收（低 $R_{sol}$），则 $Q_{solar}$ 显著增大，$p_{heat}$ 更易为正\n  - 若材料同时具备高红外发射率，则 $p_r$ 会抵消光热增益（这也是\"光热材料通常在中红外需抑制发射\"的原因之一）\n\n### 6. 论文写法示例\n\n> The net radiative heating power density was evaluated using an energy-balance model combining solar absorption, atmospheric back-radiation, convection, and outgoing thermal radiation. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. The solar absorptance $\\alpha_s$ was derived from the AM1.5-weighted reflectance spectrum, and $p_r$ and $p_a$ were computed by hemispherical integration of Planck's law over the atmospheric window (8-13 μm) using the measured material emissivity and the zenith-angle-dependent atmospheric emissivity $\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}$. The calculations were performed with angle discretization of 2000 steps (medium precision) and natural convection enabled. Heating power curves $p_{heat}(T_s)$ were reported for multiple heat-transfer coefficients to quantify the sensitivity to convection/conduction losses.\n",
  "wind_cloud": "## 模块：风速与制冷效率云图（Wind cloud）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"工具箱\"（`/tools`）→ \"风速与制冷效率云图\"（`/tools/wind-cloud`）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件（与制冷模块相同）\n\n#### 2.2 工具参数（WebUI表单）\n\n访问工具页面（`/tools/wind-cloud`）时可设置：\n\n- **风速范围**：\n  - 最小值（默认：0.0 m/s）\n  - 最大值（默认：5.0 m/s）\n  - 采样点数（默认：100，最大：200）\n- **天空发射率范围**：\n  - 最小值（默认：0.0）\n  - 最大值（默认：1.0）\n  - 采样点数（默认：100，最大：200）\n- **太阳辐照度**（可选）：可手动覆盖 `config.ini` 中的 $S_{solar}$ 值（W/m²），留空则使用配置文件中的值\n\n#### 2.3 输出结果\n\n计算完成后，页面显示：\n\n- **$\\Delta T$ 云图**：在给定（风速、天空发射率）网格上的平衡温差（°C）\n- **$h_{conv}$ 云图**：由（风速、$\\Delta T$）反推的对流换热系数（W·m⁻²·K⁻¹）\n- **交互式图表**：Plotly等高线图，可缩放、悬停查看数值\n- **数据下载**：可下载CSV格式的完整矩阵数据\n\n> **注意**：工具中的\"atmospheric emissivity（天空发射率）\"作为纵轴扫描参数，其物理含义可理解为 $\\varepsilon_{sky}\\in[0,1]$ 的参数化天空辐射强度。\n\n### 3. 核心模型（本模块的“反问题”）\n\n该模块不是直接给出 $p_{net}$，而是**求解平衡温差**：令能量平衡方程的残差为 0，解出 $\\Delta T=T_s-T_a$。\n\n在 `core/plots.py` 中定义残差函数（符号与代码一致）：\n\n$$\nf(\\Delta T;\\varepsilon_{sky},v)=\\varepsilon\\sigma\\left[(T_a+\\Delta T)^4-\\varepsilon_{sky}T_a^4\\right]+h(v,\\Delta T)\\Delta T-\\alpha_s S_{solar}\n$$\n\n其中：\n\n- $\\varepsilon$：材料平均发射率（由 `core/physics.calculate_average_emissivity` 计算，采用普朗克加权平均，例如在环境温度 $T_a$ 下\n  $$\n  \\varepsilon = \\frac{\\int I_{BB}(\\lambda,T_a)\\,\\varepsilon_s(\\lambda)\\,d\\lambda}{\\int I_{BB}(\\lambda,T_a)\\,d\\lambda}\n  $$\n  其中 $I_{BB}$ 为黑体谱辐亮度）\n- $\\alpha_s=1-R_{sol}$：太阳吸收率，其中\n  $$\n  R_{sol}=\\frac{\\int R(\\lambda)S(\\lambda)\\,d\\lambda}{\\int S(\\lambda)\\,d\\lambda}\n  $$\n  为 AM1.5 加权反射率\n- $\\sigma$：斯特藩-玻尔兹曼常数\n- $h(v,\\Delta T)$：对流换热系数（由 `core/physics.calculate_convection_coefficient` 给出）\n\n关于 $I_{BB}$ 与普朗克定律、半球积分等更完整推导，可参考辐射制冷模块文档第 3 节。\n\n#### 3.1 对流换热系数 $h(v,\\Delta T)$ 的具体计算\n\n本模块中的 $h(v,\\Delta T)$ 与辐射制冷模块中自然/强制对流换热系数使用**同一物理模型**（`core/physics.py::calculate_convection_coefficient`），只是这里风速 $v$ 不再为 0，而是由扫描网格给定。\n\n1. **物性与无量纲数**（基于膜温 $T_{film}=T_a+\\Delta T/2$）：\n   - 空气密度：$\\rho = 1.225 \\times (273.15 / T_{film})$\n   - 动力粘度：$\\mu = 1.81\\times10^{-5} (T_{film}/273.15)^{0.7}$\n   - 热导率：$k_{air} = 0.024 (T_{film}/273.15)^{0.8}$\n   - 比热容：$c_p = 1005$\n   - 运动粘度：$\\nu = \\mu/\\rho$\n   - 热扩散率：$\\alpha = k_{air}/(\\rho c_p)$\n   - 普朗特数：$\\Pr = \\nu/\\alpha$（限制在 $0.68\\sim0.75$）\n\n2. **自然对流部分**（当 $|\\Delta T|>10^{-3}$ K 时）：\n   - 体积膨胀系数：$\\beta = 1/T_{film}$\n   - 瑞利数（$L_{char}=1$ m）：\n     $$\n     Ra = \\frac{g\\beta|\\Delta T|L_{char}^3}{\\nu\\alpha}\n     $$\n   - 努塞尔数：\n     $$\n     Nu_{nat}=\n     \\begin{cases}\n     0.54\\,Ra^{0.25}, & Ra<10^7\\\\\\\\\n     0.15\\,Ra^{1/3}, & Ra\\ge 10^7\n     \\end{cases}\n     $$\n   - 自然对流换热系数：$h_{natural}=Nu_{nat}k_{air}/L_{char}$。\n\n3. **强制对流部分**（当风速 $v>10^{-3}$ m/s 时）：\n   - 雷诺数：$Re=vL_{char}/\\nu$\n   - 努塞尔数：\n     $$\n     Nu_{forced}=\n     \\begin{cases}\n     0.664\\,Re^{0.5}\\Pr^{1/3}, & Re<5\\times10^5\\\\\\\\\n     0.037\\,Re^{0.8}\\Pr^{1/3}, & Re\\ge 5\\times10^5\n     \\end{cases}\n     $$\n   - 强制对流换热系数：$h_{forced}=Nu_{forced}k_{air}/L_{char}$。\n\n4. **Churchill–Usagi 混合**（幂次 $n=3$）：\n   $$\n   h(v,\\Delta T)=h_{conv,mixed}=\\left(h_{natural}^3+h_{forced}^3\\right)^{1/3}\n   $$\n   并施加下限 $h(v,\\Delta T)\\ge 1.0$ W·m$^{-2}$·K$^{-1}$。\n\n随后对每个网格点 $(\\varepsilon_{sky},v)$：\n\n- 优先用 `brentq` 在 $[\\Delta T_{min},\\Delta T_{max}]$ 上求根\n- 若无符号变化导致无法夹逼，则用最小化 $|f|$ 的方式求近似解\n\n### 4. 计算步骤\n\n1. 由光谱数据计算材料参数：$\\varepsilon$（普朗克加权平均）与 $\\alpha_s$（AM1.5加权）。\n2. 构造扫描网格：天空发射率 $\\varepsilon_{sky}\\in[0,1]$，风速 $v\\in[0,5]$ m/s（默认 50×50）。\n3. 对每个网格点求解平衡温差 $\\Delta T$（使 $f(\\Delta T)=0$）。\n4. 用得到的 $\\Delta T$ 与 $v$ 反算 $h_{conv}$，输出两张云图（$\\Delta T$ 与 $h_{conv}$）。\n\n### 5. 小案例（可复现）\n\n**目标**：在 $S_{solar}$ 为 `config.ini` 值（例如 1000 W/m²）下，得到\"风速-天空发射率\"平衡温差云图。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **访问工具**：\n\n   - 访问\"工具箱\"页面（`/tools`）\n   - 点击\"风速与制冷效率云图\"卡片，或直接访问 `/tools/wind-cloud`\n3. **设置参数**：\n\n   - 风速范围：0.0 ~ 5.0 m/s（默认）\n   - 天空发射率范围：0.0 ~ 1.0（默认）\n   - 采样点数：100×100（默认，可根据需要调整，最大200×200）\n   - 太阳辐照度：留空（使用配置文件中的值）或手动输入（如 1000 W/m²）\n4. **执行计算**：\n\n   - 点击\"计算\"按钮\n   - 等待计算完成（通常几秒到几十秒，取决于网格分辨率）\n5. **查看结果**：\n\n   - **$\\Delta T$ 云图**：等高线图显示不同（风速、天空发射率）组合下的平衡温差\n   - **$h_{conv}$ 云图**：显示对应的对流换热系数\n   - **交互式操作**：可缩放、悬停查看精确数值、切换等高线密度\n   - **下载数据**：点击\"导出CSV\"下载完整矩阵数据\n\n**如何读图**：\n\n- **$\\Delta T<0$**：表示表面温度低于环境温度（有制冷潜力）\n- **风速影响**：风速越大，$h_{conv}$ 越大，对流损失越强，通常会使 $|\\Delta T|$ 变小（更难\"降温\"）\n- **天空发射率影响**：天空发射率越小（天空更\"冷\"、更晴朗），越有利于负温差\n- **边界线**：$\\Delta T=0$ 的等高线可视作\"制冷/制热分界线\"\n\n### 6. 论文写法示例 \n\n> To visualize the sensitivity of the equilibrium sub-ambient temperature to wind and sky conditions, we solved a steady-state energy balance for $\\Delta T=T_s-T_a$ on a 2D grid of wind speed $v$ and effective sky emissivity $\\varepsilon_{sky}$. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. The balance residual was defined as $f(\\Delta T)=\\varepsilon\\sigma[(T_a+\\Delta T)^4-\\varepsilon_{sky}T_a^4]+h(v,\\Delta T)\\Delta T-\\alpha_s S_{solar}$, where $\\alpha_s$ was obtained from AM1.5-weighted reflectance, $\\varepsilon$ was the Planck-weighted average emissivity, and $h(v,\\Delta T)$ was evaluated using a mixed natural/forced convection correlation (Churchill-Usagi method). The root $\\Delta T$ was computed numerically at each grid point using Brent's method or minimization of $|f|$ to generate contour maps of $\\Delta T$ and the corresponding $h_{conv}$. The grid resolution was set to 100×100 points, covering wind speeds from 0 to 5 m/s and sky emissivity from 0 to 1.\n",
  "solar_efficiency": "## 模块：光热转化效率计算（理论光热 vs 光照）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"工具箱\"（`/tools`）→ \"光热转化效率计算\"（`/tools/solar-efficiency`）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件（与制冷模块相同）\n\n#### 2.2 工具参数（WebUI表单）\n\n访问工具页面（`/tools/solar-efficiency`）时可设置：\n\n- **角度积分步数**（Angle Steps）：默认 2000（与桌面版保持一致）\n- **环境温度范围**：\n  - 最小值（默认：-100°C）\n  - 最大值（默认：100°C）\n  - 采样点数（默认：100，最大：200）\n- **太阳辐照度范围**：\n  - 最小值（默认：0 W/m²）\n  - 最大值（默认：1200 W/m²）\n  - 采样点数（默认：100，最大：200）\n\n#### 2.3 输出结果\n\n计算完成后，页面显示：\n\n- **理论净制热功率云图**：在 $T_a$ 与 $S_{solar}$ 二维网格上的理论净辐射制热功率（W/m²）\n- **交互式图表**：Plotly等高线图或热图，可缩放、悬停查看数值\n- **数据下载**：可下载CSV格式的完整矩阵数据（行为 $T_a$，列为 $S_{solar}$）\n\n### 3. 模型假设（本模块是“快速理论扫描”）\n\n该模块用于生成“环境温度×太阳辐照度”的关系图（或热图），采用两点关键简化：\n\n- **薄膜温度取环境温度**：$\\Delta T=0\\Rightarrow T_s=T_a$\n- **角度处理做平均近似**：大气角度相关发射率 $\\varepsilon_{atm}(\\lambda,\\theta)$ 用一个代表性平均角度的均值近似（加速计算）\n\n因此本模块输出可视作**上界/趋势图**，用于科研讨论与参数敏感性分析，而非严格逐角积分的高精度曲线。\n\n### 4. 核心公式\n\n在 $\\Delta T=0$ 条件下，忽略对流项（因为 $T_s=T_a$）：\n\n$$\np_{heat,\\;theory}(T_a,S)=Q_{solar}(S)+p_a(T_a)-p_r(T_a)\n$$\n\n其中：\n\n- $Q_{solar}=\\alpha_s S$，$\\alpha_s=1-R_{sol}$\n- $p_r$：材料向外长波辐射（波长积分+半球角积分的简化形式）\n- $p_a$：大气向下长波辐射（采用平均角度的 $\\varepsilon_{atm}$ 近似）\n\n### 5. 计算步骤\n\n1. 计算 $R_{sol}$ 与 $\\alpha_s$。\n2. 读取并插值 $\\varepsilon_s(\\lambda)$、$\\tau(\\lambda)$。\n3. 构造温度网格 $T_a$ 与辐照度网格 $S_{solar}$。\n4. 对每个 $T_a$：\n   - 计算 $p_r(T_a)$、$p_a(T_a)$（数值稳定普朗克项+波长积分+角度平均近似）\n   - 向量化叠加 $Q_{solar}=\\alpha_s S_{solar}$，得到 `results[i,:]`\n\n### 6. 小案例（可复现）\n\n**目标**：绘制不同环境温度下，\"净制热功率 vs 太阳辐照度\"的关系云图。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **访问工具**：\n\n   - 访问\"工具箱\"页面（`/tools`）\n   - 点击\"光热转化效率计算\"卡片，或直接访问 `/tools/solar-efficiency`\n3. **设置参数**：\n\n   - 环境温度范围：-100 ~ 100°C（默认，可根据需要调整）\n   - 太阳辐照度范围：0 ~ 1200 W/m²（默认）\n   - 采样点数：100×100（默认，最大200×200）\n   - 角度积分步数：2000（默认，保持与桌面版一致）\n4. **执行计算**：\n\n   - 点击\"计算\"按钮\n   - 等待计算完成（通常几秒到几十秒，取决于网格分辨率）\n5. **查看结果**：\n\n   - **理论净制热功率云图**：等高线图或热图显示不同（$T_a$、$S_{solar}$）组合下的理论净制热功率\n   - **交互式操作**：可缩放、悬停查看精确数值、切换等高线密度\n   - **下载数据**：点击\"导出CSV\"下载完整矩阵数据（行为 $T_a$，列为 $S_{solar}$，单元格为 $p_{heat,\\;theory}$）\n\n**结果解读**：\n\n- **斜率接近 $\\alpha_s$**：因为 $Q_{solar}=\\alpha_s S$ 是随辐照度线性增长的主项\n- **截距由 $p_a-p_r$ 决定**：反映同温条件下长波辐射的\"净增热/净散热\"趋势\n- **等高线分布**：可用于识别\"净制热/净散热\"的边界条件\n\n### 7. 论文写法示例\n\n> For a rapid parametric assessment, we computed a theoretical net radiative heating power assuming zero temperature difference ($T_s=T_a$). The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. The net heating power was expressed as $p_{heat}= \\alpha_s S_{solar} + p_a(T_a) - p_r(T_a)$, where $\\alpha_s$ was obtained from the AM1.5-weighted reflectance. Atmospheric back-radiation was approximated using an effective angular-averaged atmospheric emissivity derived from the measured zenith transmittance spectrum, enabling fast evaluation over a 2D grid of ambient temperature ($T_a$ from -100 to 100°C) and solar irradiance ($S_{solar}$ from 0 to 1200 W/m²) with 100×100 resolution. The calculations were performed with angle discretization of 2000 steps to maintain consistency with high-precision calculations.\n",
  "emissivity_solar_cloud": "## 模块：大气发射率 - 太阳光强云图（Atmospheric emissivity vs Solar irradiance）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"工具箱\"（`/tools`）→ \"大气发射率-太阳光强云图\"（`/tools/emissivity-solar`）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件（与制冷模块相同）\n\n#### 2.2 工具参数（WebUI表单）\n\n访问工具页面（`/tools/emissivity-solar`）时可设置：\n\n- **大气发射率采样点数**：默认 100，最大 200（扫描范围：0 ~ 1）\n- **太阳辐照度采样点数**：默认 100，最大 200（扫描范围：0 ~ `solar_max`）\n- **最大太阳辐照度**（`solar_max`）：默认 1000 W/m²\n\n#### 2.3 输出结果\n\n计算完成后，页面显示：\n\n- **净制冷功率云图**：在 $(\\varepsilon_{atm}, S_{solar})$ 二维网格上的净制冷功率（W/m²）\n- **0 功率边界线**：等高线图标注 $P_{cool}=0$ 的分界线（净制冷/净制热分界）\n- **交互式图表**：Plotly等高线图，可缩放、悬停查看数值\n- **材料参数显示**：图表标题会显示当前材料的 $\\varepsilon$、$\\alpha_s$ 和 $T_a$\n- **数据下载**：可下载CSV格式的完整矩阵数据\n\n### 3. 模型假设（用于做“相图/边界”）\n\n该模块刻意采用极简稳态模型，方便科研展示“参数边界”：\n\n- **固定 $\\Delta T=0$**：$T_{film}=T_a$\n- **不做光谱积分**：使用材料平均发射率 $\\varepsilon$（由主模块计算得到）\n- **大气用一个标量 $\\varepsilon_{atm}$** 参数化（不使用 $\\tau(\\lambda,\\theta)$ 的逐谱逐角模型）\n\n因此该云图用于解释“在给定材料 $\\varepsilon,\\alpha_s$ 时，哪些 $(\\varepsilon_{atm},S_{solar})$ 区域有正净制冷功率”。\n\n### 4. 核心公式\n\n令 $T_{film}=T_a$，则：\n\n- 材料向外长波辐射（斯特藩-玻尔兹曼）：\n\n$$\nP_{rad,out}=\\varepsilon\\sigma T_a^4\n$$\n\n- 大气向下辐射（按 $\\varepsilon_{atm}$）：\n\n$$\nP_{rad,in}=\\varepsilon\\,\\varepsilon_{atm}\\sigma T_a^4\n$$\n\n- 太阳吸收：\n\n$$\nP_{solar}=\\alpha_s S_{solar}\n$$\n\n得到云图中的“净制冷功率”（代码中命名 `P_cooling`）：\n\n$$\nP_{cool}=P_{rad,out}-P_{rad,in}-P_{solar}\n=\\varepsilon\\sigma T_a^4\\left(1-\\varepsilon_{atm}\\right)-\\alpha_s S_{solar}\n$$\n\n### 5. 计算步骤\n\n1. 调用 `core.calculations.main_calculating_gui()` 得到材料平均发射率 $\\varepsilon$ 与 $\\alpha_s$。\n2. 对 $\\varepsilon_{atm}$ 与 $S_{solar}$ 建立二维网格。\n3. 在每个网格点用上式计算 $P_{cool}$，绘制云图与 0 等值线。\n\n### 6. 小案例（可复现）\n\n**目标**：绘制 $\\varepsilon_{atm}\\in[0,1]$、$S_{solar}\\in[0,1000]$ 下的 $P_{cool}$ 相图，并读取\"0 等值线\"作为可行边界。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **访问工具**：\n\n   - 访问\"工具箱\"页面（`/tools`）\n   - 点击\"大气发射率-太阳光强云图\"卡片，或直接访问 `/tools/emissivity-solar`\n3. **设置参数**：\n\n   - 大气发射率采样点数：100（默认，最大200）\n   - 太阳辐照度采样点数：100（默认，最大200）\n   - 最大太阳辐照度：1000 W/m²（默认）\n4. **执行计算**：\n\n   - 点击\"计算\"按钮\n   - 等待计算完成（通常几秒）\n5. **查看结果**：\n\n   - **净制冷功率云图**：等高线图显示不同（$\\varepsilon_{atm}$、$S_{solar}$）组合下的净制冷功率\n   - **0 功率边界线**：等高线图标注 $P_{cool}=0$ 的分界线（净制冷/净制热分界）\n   - **材料参数显示**：图表标题会显示当前材料的 $\\varepsilon$、$\\alpha_s$ 和 $T_a$\n   - **交互式操作**：可缩放、悬停查看精确数值\n   - **下载数据**：点击\"导出CSV\"下载完整矩阵数据\n\n**读图方式**：\n\n- **0 等值线以上/以下**：分别对应净制冷/净制热区域（取决于色标方向）\n- **临界太阳辐照度**：在给定 $\\varepsilon_{atm}$ 下，0 线给出使 $P_{cool}=0$ 的临界太阳辐照度：\n\n$$\nS_{crit}=\\frac{\\varepsilon\\sigma T_a^4(1-\\varepsilon_{atm})}{\\alpha_s}\n$$\n\n- **材料优化方向**：高 $\\varepsilon$、低 $\\alpha_s$ 的材料在更宽的（$\\varepsilon_{atm}$、$S_{solar}$）范围内具有净制冷能力\n\n### 7. 论文写法示例\n\n> To illustrate the operating envelope, we constructed a two-parameter \"cooling map\" as a function of effective atmospheric emissivity $\\varepsilon_{atm}$ and solar irradiance $S$, assuming $T_s=T_a$. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. Using the material's Planck-weighted emissivity $\\varepsilon$ and AM1.5-derived solar absorptance $\\alpha_s$, the net power was computed by $P_{cool}=\\varepsilon\\sigma T_a^4(1-\\varepsilon_{atm})-\\alpha_s S$. The calculation was performed on a 100×100 grid covering $\\varepsilon_{atm}\\in[0,1]$ and $S_{solar}\\in[0,1000]$ W/m². The $P_{cool}=0$ contour provides the boundary between net cooling and net heating regimes, enabling identification of the critical solar irradiance $S_{crit}=\\varepsilon\\sigma T_a^4(1-\\varepsilon_{atm})/\\alpha_s$ for any given atmospheric condition.\n",
  "power_components": "## 模块：功率分量曲线图（Power components）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"工具箱\"（`/tools`）→ \"功率分量曲线图\"（`/tools/power-components`）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件（与制冷/制热模块相同）\n\n#### 2.2 工具参数（WebUI表单）\n\n访问工具页面（`/tools/power-components`）时可设置：\n\n- **角度积分步数**（Angle Steps）：默认 2000\n- **等效换热系数**（`h_cond_wm2k`）：默认 5.0 W·m⁻²·K⁻¹（用于模拟额外的导热/对流损失）\n- **自然对流**（Natural Convection）：是否启用自然对流计算（默认：启用）\n- **相变参数**（可选）：\n  - 相变起始温度（°C）\n  - 相变功率（W/m²，默认：0）\n  - 相变半宽度（°C，默认：0）\n\n#### 2.3 输出结果\n\n计算完成后，页面显示：\n\n- **功率分量曲线图**：多条曲线显示随 $T_{film}$ 变化的各功率分量（W/m²）：\n  - `p_r`：材料向外长波辐射\n  - `p_a`：大气向下长波辐射\n  - `Q_nat`：自然对流功率\n  - `Q_cond`：等效导热/换热功率\n  - `Q_conv`：总对流/换热功率（$Q_{conv}=Q_{nat}+Q_{cond}$）\n  - `Q_solar`：太阳吸收功率\n  - `P_phase`：相变功率（如启用）\n  - `p_net`：净制冷功率\n- **交互式图表**：Plotly多线图，可缩放、悬停查看数值、切换曲线显示\n- **数据下载**：可下载CSV格式的完整数据（包含所有分量）\n\n### 3. 核心公式（与制冷主模块一致，但拆分输出）\n\n#### 3.1 长波辐射分量\n\n- $p_r$：材料向外辐射（谱积分+半球角积分）\n- $p_a$：大气回辐射（$\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}$）\n\n#### 3.2 对流/换热分解\n\n自然对流：\n\n$$\nQ_{nat}=h_{nat}(T_a-T_{film})\n$$\n\n等效导热/对流（用户输入常数）：\n\n$$\nQ_{cond}=h_{cond}(T_a-T_{film})\n$$\n\n总换热：\n\n$$\nQ_{conv}=Q_{nat}+Q_{cond}\n$$\n\n#### 3.3 太阳吸收与净制冷功率\n\n$$\nQ_{solar}=\\alpha_s S_{solar},\\quad \\alpha_s=1-R_{sol}\n$$\n\n$$\np_{net}=p_r-p_a-Q_{conv}-Q_{solar}+P_{phase}\n$$\n\n### 4. 计算步骤\n\n1. 读取光谱得到 $R_{sol}$ 与 $\\alpha_s$。\n2. 读取材料发射率与大气透过率，计算 $p_r$、$p_a$。\n3. 对每个 $T_{film}$：\n   - 计算 $h_{nat}(T_{film})$（基于温差的自然对流相关式）\n   - 计算 $Q_{nat}$、$Q_{cond}$、$Q_{conv}$\n   - 合成 $p_{net}$\n4. 输出各分量曲线并允许导出 CSV（GUI提供导出按钮）。\n\n### 5. 小案例（可复现）\n\n**目标**：把\"为什么某个材料净制冷功率不高\"拆解为：是 $p_r$ 不够？还是 $p_a$ 太大？还是 $Q_{solar}$ 太大？或 $Q_{conv}$ 损失太强？\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **访问工具**：\n\n   - 访问\"工具箱\"页面（`/tools`）\n   - 点击\"功率分量曲线图\"卡片，或直接访问 `/tools/power-components`\n3. **设置参数**：\n\n   - 等效换热系数：5.0 W·m⁻²·K⁻¹（默认）\n   - 自然对流：启用（默认）\n   - 角度积分步数：2000（默认）\n   - 相变参数：如不需要，保持默认值（0）\n4. **执行计算**：\n\n   - 点击\"计算\"按钮\n   - 等待计算完成（通常几秒）\n5. **查看结果**：\n\n   - **功率分量曲线图**：多条曲线显示各功率分量随 $T_{film}$ 的变化\n   - **交互式操作**：\n     - 可缩放、悬停查看精确数值\n     - 点击图例可切换各曲线的显示/隐藏\n     - 可对比不同分量的相对大小\n   - **下载数据**：点击\"导出CSV\"下载完整数据（包含 `p_r`, `p_a`, `Q_solar`, `Q_conv`, `p_net` 等所有分量）\n\n**结果解读**：\n\n- **$p_r$ 曲线**：材料向外长波辐射，随 $T_{film}$ 升高而增大\n- **$p_a$ 曲线**：大气向下长波辐射，与 $T_a$ 相关，不随 $T_{film}$ 变化\n- **$Q_{solar}$ 曲线**：太阳吸收功率，与 $S_{solar}$ 相关，不随 $T_{film}$ 变化\n- **$Q_{conv}$ 曲线**：对流/换热损失，随 $T_{film}$ 升高（$\\Delta T$ 增大）而增大\n- **$p_{net}$ 曲线**：净制冷功率，由各分量合成；$p_{net}=0$ 的交点对应平衡温度\n\n### 6. 论文写法示例\n\n> We decomposed the net cooling power into individual contributions to identify performance-limiting factors. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. The net power density was computed as $p_{net}=p_r-p_a-Q_{conv}-Q_{solar}$ (plus an optional phase-change term $P_{phase}$), where $p_r$ and $p_a$ were calculated by hemispherical integration of Planck's law over the atmospheric window, $Q_{solar}=\\alpha_s S_{solar}$ was derived from AM1.5-weighted reflectance, and $Q_{conv}=Q_{nat}+Q_{cond}$ combined natural convection (computed using Churchill-Usagi method) and an equivalent conduction coefficient ($h_{cond}=5.0$ W m$^{-2}$ K$^{-1}$). The calculations were performed with angle discretization of 2000 steps. Component-wise curves as a function of film temperature provide mechanistic insights into whether performance is limited by solar absorption, atmospheric back radiation, or heat-transfer losses.\n",
  "angular_power": "## 模块：天空窗口角分辨分析（Angular profile）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"工具箱\"（`/tools`）→ \"天空窗口角分辨分析\"（`/tools/angular-power`）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件\n\n#### 2.2 工具参数（WebUI表单）\n\n访问工具页面（`/tools/angular-power`）时可设置：\n\n- **温差**（`temp_diff_c`）：$\\Delta T=T_s-T_a$（°C），默认 0.0\n- **角度采样数**（`angle_steps`）：角度离散数，默认 91（对应 0–90°，步长 1°）\n\n#### 2.3 输出结果\n\n计算完成后，页面显示：\n\n- **角分辨功率密度曲线**：直角坐标图显示单位立体角的净辐射功率密度（W·m⁻²·sr⁻¹）随天顶角（度）的变化\n- **极坐标图**（可选）：极坐标可视化显示角分辨功率分布\n- **交互式图表**：Plotly图表，可缩放、悬停查看数值\n- **数据下载**：可下载CSV格式的完整数据（包含 `theta_deg` 和 `power_density_per_sr`）\n\n### 3. 核心物理意义\n\n该模块回答一个科研常见问题：\n\n> 材料的“向天空窗口辐射/散热”在不同角度方向上分布如何？\n\n输出的 $W·m^{-2}·sr^{-1}$ 是“辐射亮度（radiance）级别”的量，描述在某个天顶角方向上，每单位立体角贡献的净辐射功率密度。\n\n### 4. 核心公式\n\n#### 4.1 天顶角相关的大气发射率\n\n$$\n\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}\n$$\n\n#### 4.2 表面与大气的谱辐亮度\n\n代码先计算谱辐出射度（exitance）再除以 $\\pi$ 得谱辐亮度：\n\n$$\nL_{BB}(\\lambda,T)=\\frac{M_{BB}(\\lambda,T)}{\\pi}\n$$\n\n#### 4.3 角分辨净谱辐亮度\n\n$$\nL_{net}(\\lambda,\\theta)=\\varepsilon_s(\\lambda)\\left[L_{BB}(\\lambda,T_s)-\\varepsilon_{atm}(\\lambda,\\theta)L_{BB}(\\lambda,T_a)\\right]\n$$\n\n对波长积分（数值求和）得到该角度方向的净辐射功率密度（单位立体角）：\n\n$$\nP(\\theta)=\\int L_{net}(\\lambda,\\theta)\\,d\\lambda\n\\quad [\\mathrm{W\\,m^{-2}\\,sr^{-1}}]\n$$\n\n### 5. 计算步骤\n\n1. 从 `wavelength.csv` 建立统一波长网格，插值材料发射率与大气透过率。\n2. 给定 $T_a$（来自 `config.ini`）与 $\\Delta T$，得到 $T_s=T_a+\\Delta T$。\n3. 对每个天顶角 $\\theta$：\n   - 计算 $\\varepsilon_{atm}(\\lambda,\\theta)$\n   - 计算并积分得到 $P(\\theta)$\n4. 输出直角坐标与极坐标两种可视化。\n\n### 6. 小案例（可复现）\n\n**目标**：观察在 $\\Delta T=0^\\circ C$ 时，各角度方向的净辐射是否为正/负，以及是否存在\"角度更有利的天空窗口\"。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传发射率文件（`emissivity.txt`）\n   - 确认文件已处理完成并处于\"激活\"状态\n   - 注意：该模块不需要反射率文件\n2. **访问工具**：\n\n   - 访问\"工具箱\"页面（`/tools`）\n   - 点击\"天空窗口角分辨分析\"卡片，或直接访问 `/tools/angular-power`\n3. **设置参数**：\n\n   - 温差（$\\Delta T$）：0.0°C（默认，表示 $T_s=T_a$）\n   - 角度采样数：91（默认，对应 0–90°，步长 1°）\n4. **执行计算**：\n\n   - 点击\"计算\"按钮\n   - 等待计算完成（通常几秒）\n5. **查看结果**：\n\n   - **角分辨功率密度曲线**：直角坐标图显示各天顶角方向的净辐射功率密度\n   - **极坐标图**（如提供）：极坐标可视化显示角分辨功率分布\n   - **交互式操作**：可缩放、悬停查看精确数值\n   - **下载数据**：点击\"导出CSV\"下载完整数据\n6. **对比分析**：\n\n   - 可进一步尝试 $\\Delta T=-5^\\circ C$（表面更冷）与 $+5^\\circ C$（表面更热）进行对比\n   - 观察不同温差下各角度方向的净辐射功率变化\n\n**结果解读**：\n\n- **$P(\\theta)>0$**：该角度方向有净向外辐射（有利于制冷）\n- **$P(\\theta)<0$**：该角度方向有净向内辐射（不利于制冷）\n- **角度分布**：通常天顶角较小（接近垂直向上）的方向更有利于辐射制冷，因为大气透过率更高\n- **温差影响**：$\\Delta T<0$ 时，各角度方向的净辐射功率通常更大（更有利于制冷）\n\n### 7. 论文写法示例\n\n> To analyze the directional radiative exchange within the atmospheric window, we computed the zenith-angle-resolved net radiative power density $P(\\theta)$ (W m$^{-2}$ sr$^{-1}$). The material's emissivity spectrum was measured and uploaded to the calculation platform. The angle-dependent atmospheric emissivity was modeled as $\\varepsilon_{atm}(\\lambda,\\theta)=1-\\tau(\\lambda)^{1/\\cos\\theta}$ from the measured zenith transmittance $\\tau(\\lambda)$, accounting for the increased path length at larger zenith angles. The net spectral radiance was calculated as $L_{net}(\\lambda,\\theta)=\\varepsilon_s(\\lambda)[L_{BB}(\\lambda,T_s)-\\varepsilon_{atm}(\\lambda,\\theta)L_{BB}(\\lambda,T_a)]$, where $L_{BB}(\\lambda,T)$ is the blackbody spectral radiance, and integrated over wavelength to obtain $P(\\theta)=\\int L_{net}(\\lambda,\\theta)d\\lambda$. The calculation was performed with 91 angle samples covering 0–90° zenith angles, enabling identification of the most favorable angular directions for radiative cooling.\n",
  "in_situ_era5": "## 模块：原位模拟（in-situ simulation）\n\n### 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"新建任务\" → 选择任务类型为 `in_situ_simulation`（原位模拟）\n\n### 2. 输入/输出\n\n#### 2.1 前置准备（材料文件上传）\n\n用户需要先上传材料文件：\n\n1. **上传反射率文件**：访问\"上传\"页面，上传 `reflectance.txt`\n2. **上传发射率文件**：上传 `emissivity.txt`\n3. **确认激活状态**：上传后文件会自动处理并设置为\"激活\"状态\n4. **材料参数自动计算**：系统会从材料光谱自动计算：\n   - $\\varepsilon$：材料长波发射率（普朗克加权平均发射率）\n   - $\\rho_{solar}$：太阳反射率（AM1.5加权反射率 $R_{sol}$）\n\n#### 2.2 任务参数（WebUI表单）\n\n创建任务时可在Web界面设置：\n\n- **日期范围**：\n  - 开始日期（`start_date`）：YYYY-MM-DD 格式\n  - 结束日期（`end_date`）：YYYY-MM-DD 格式\n  - **限制**：不支持跨年，日期范围最多2个月\n- **地理位置**：\n  - 经度（`lon`）：-180 ~ 180\n  - 纬度（`lat`）：-90 ~ 90\n  - 时区偏移（`tz_offset_hours`）：默认 8（北京时间）\n  - **自动定位**：可点击\"自动定位\"按钮使用浏览器定位功能\n- **材料参数**（可选覆盖）：\n  - 材料发射率（$\\varepsilon$）：如留空，使用从材料光谱计算的值\n  - 太阳反射率（$\\rho_{solar}$）：如留空，使用从反射率谱计算的值\n- **其他参数**：\n  - 天空视角系数（`sky_view`）：0–1，默认 1.0（用于遮挡/建筑遮蔽情形的等效缩放）\n  - 使用经验大气模型（`use_empirical_atm`）：是否使用经验式估算天空发射率（默认：否，优先使用真实数据）\n- **图表生成选项**：可选择生成哪些统计图表（默认启用组1、组2和部分组3、组4图表）\n\n#### 2.3 羲和气象数据\n\n系统会自动从羲和气象平台下载所需气象字段（合并CSV中包含）：\n\n- `t2m`（2 m 气温，K）、`d2m`（2 m 露点，K）、`u10`/`v10`（10 m 风分量）\n- `tcc`（总云量，0–1）\n- `ssrd`（地表下行短波辐射，J/m²/step）\n- `strd`（地表下行长波辐射，J/m²/step）\n\n#### 2.4 输出结果\n\n任务完成后，在任务详情页可查看：\n\n- **时间序列图表**：多张统计图表（Plotly交互式图表）：\n  - 组1：制冷功率时间序列、功率分量、温度云图、风速湿度等\n  - 组2：小时变化、功率组成、昼夜对比、制冷vs温度/风速/湿度等\n  - 组3：分布图、累积图、材料vs理想对比、热图等\n  - 组4：优化分析、相关性矩阵、湿度影响、推荐等\n  - 组5：KPI卡片、时间序列、分布、效率、透过率、汇总表等\n- **摘要数据**（Summary）：关键统计指标\n- **数据下载**：可下载CSV格式的完整计算结果（逐时间步的净制冷功率与分量等）\n\n### 3. 核心公式（项目当前实现）\n\n#### 3.1 相对湿度与辐照度单位换算\n\n相对湿度（由温度与露点推算，Tetens）：\n\n$$\nRH = 100\\times \\frac{e(T_d)}{e(T)},\\quad e(T)=610.8\\exp\\left(\\frac{17.27T}{T+237.3}\\right)\n$$\n\n辐射为“累积能量”（J/m²/step），换算为功率：\n\n$$\nG=\\frac{E}{\\Delta t}\n\\quad [\\mathrm{W/m^2}]\n$$\n\n#### 3.2 天空发射率（经验式，Brutsaert+云量修正多项式）\n\n代码使用：\n\n$$\n\\varepsilon_{clr}=1.24\\left(\\frac{e_a}{T_K}\\right)^{1/7}\n$$\n\n并将云量 $C\\in[0,1]$ 映射为 $N=10C$，用多项式修正：\n\n$$\ncorr = 1 + 0.0224N - 0.0035N^2 + 0.00028N^3\n$$\n\n$$\n\\varepsilon_{sky}=\\varepsilon_{clr}\\cdot corr,\\quad \\tau_{atm}=1-\\varepsilon_{sky}\n$$\n\n#### 3.3 净制冷功率（当前实现为“表面温度≈近地面气温”的近似）\n\n在 `new_module/radiative_cooling_from_weather_csv.py` 中：\n\n- 表面温度取 $T_s \\approx T_{2m}$，大气温度 $T_a\\approx T_{2m}$\n\n材料长波辐射：\n\n$$\nP_{emit}=\\varepsilon\\sigma T_s^4\n$$\n\n大气向下长波辐射：\n\n- 若直接使用 `strd`（推荐）：\n\n$$\nP_{atm}=\\varepsilon \\cdot L^{\\downarrow}_{real}\n$$\n\n- 若缺失则回退到经验式：\n\n$$\nP_{atm}=\\varepsilon(1-\\tau_{atm})\\sigma T_a^4\n$$\n\n- 可选混合模式（0.3经验 + 0.7 真实数据）用于稳健性：\n\n$$\nP_{atm}=0.5\\cdot \\varepsilon(1-\\tau_{atm})\\sigma T_a^4 + 0.5\\cdot \\varepsilon L^{\\downarrow}_{real}\n$$\n\n太阳吸收：\n\n$$\nP_{solar}=(1-\\rho_{solar})\\cdot G^{\\downarrow}_{real}\n$$\n\n得到净制冷功率：\n\n$$\nP_{net}=P_{emit}-P_{atm}-P_{solar}\n$$\n\n### 4. 计算步骤\n\n1. **下载**：按日期与经纬度从羲和平台拉取天气文件。\n2. **预处理**：\n   - 由 `t2m`/`d2m` 得到 RH\n   - 将 `ssrd/strd` 从 J/m² 转为 W/m²\n   - 由云量与 RH 估计 $\\tau_{atm}$（用于缺测回退与诊断）\n3. **逐时间步计算**：按 3.3 的分量公式计算 $P_{net}$ 与统计量，并生成图表。\n\n### 5. 小案例（可复现）\n\n**目标**：对某一地点 2 天数据评估净制冷功率时间序列。\n\n**操作步骤**（WebUI）：\n\n1. **准备材料文件**：\n\n   - 访问\"上传\"页面（`/uploads`）\n   - 上传反射率文件和发射率文件\n   - 确认两个文件都已处理完成并处于\"激活\"状态\n2. **创建任务**：\n\n   - 访问\"新建任务\"页面（`/jobs/new?type=in_situ_simulation`）\n   - 任务类型自动设置为\"原位模拟\"（in_situ_simulation）\n   - **注意**：需要高级用户或管理员权限\n3. **设置参数**：\n\n   - **日期范围**：\n     - 开始日期：`2025-07-01`\n     - 结束日期：`2025-07-02`\n   - **地理位置**：\n     - 经度：`117.2695`（或点击\"自动定位\"使用浏览器定位）\n     - 纬度：`31.8369`\n     - 时区偏移：`8`（北京时间）\n   - **材料参数**：\n     - 材料发射率：留空（使用从材料光谱自动计算的值，如 $\\varepsilon=0.98$）\n     - 太阳反射率：留空（使用从反射率谱自动计算的值，如 $\\rho_{solar}=0.91$）\n   - **其他参数**：\n     - 天空视角系数：`1.0`（默认）\n     - 使用经验大气模型：否（默认，优先使用真实数据）\n   - **图表生成选项**：选择需要生成的图表组（默认启用组1、组2和部分组3、组4）\n4. **提交任务**：\n\n   - 点击\"提交任务\"按钮\n   - 系统会自动：\n     - 从羲和平台下载所需气象数据\n     - 合并数据\n     - 计算净制冷功率\n     - 生成统计图表\n5. **查看结果**：\n\n   - 任务提交后自动跳转到任务详情页\n   - 等待任务完成（状态从 `queued` → `started` → `succeeded`，可能需要几分钟到几十分钟，取决于日期范围）\n   - 在详情页查看：\n     - **多张统计图表**：Plotly交互式图表，包括时间序列、分布、相关性等\n     - **摘要信息**：关键统计指标\n     - **下载数据**：点击\"导出CSV\"下载完整计算结果\n\n**结果解读**：\n\n- **夜间（低短波）**：$P_{solar}\\approx0$ 时，$P_{net}$ 更能代表\"纯辐射制冷潜力\"\n- **白天**：$P_{solar}$ 会显著拉低 $P_{net}$，材料高反射率（高 $\\rho_{solar}$）是关键\n- **时间序列分析**：可观察净制冷功率的日变化、昼夜对比等\n- **统计分布**：可分析净制冷功率的分布特征、累积概率等\n\n### 6. 论文写法示例\n\n> In-situ performance was evaluated using in-situ reanalysis data at the target location and period. The material's reflectance and emissivity spectra were measured and uploaded to the calculation platform. Material parameters were automatically computed: the Planck-weighted average emissivity $\\varepsilon$ and the AM1.5-weighted solar reflectance $\\rho_{solar}$. Downwelling shortwave (ssrd) and longwave (strd) radiation were downloaded from Xihe-Platform and converted from accumulated energy (J m$^{-2}$) to irradiance (W m$^{-2}$) using the time-step duration. Relative humidity was derived from 2-m air temperature and dewpoint temperature using the Tetens equation. The net cooling power density was estimated as $P_{net}=\\varepsilon\\sigma T_s^4-\\varepsilon L^{\\downarrow}- (1-\\rho_{solar})G^{\\downarrow}$, where $L^{\\downarrow}$ and $G^{\\downarrow}$ are the downwelling longwave and shortwave irradiances, respectively, $T_s\\approx T_{2m}$ was approximated as the 2-m air temperature, and $\\sigma$ is the Stefan-Boltzmann constant. When longwave data were unavailable, an empirical sky-emissivity model based on humidity and cloud cover (Brutsaert formula with cloud correction) was used to approximate atmospheric back radiation. The simulation covered [start_date] to [end_date] at coordinates [lon, lat] with a time-zone offset of [tz_offset] hours.\n",
  "material_comparison": "# 功量地图绘制（Power Map）\n\n## 1. 模块入口与位置\n\n- **WebUI入口**：网页界面 → \"功量地图绘制\"（`/power-map`）\n- **权限要求**：仅限 Pro 用户使用\n\n## 2. 输入/输出\n\n### 2.1 任务参数（WebUI表单）\n\n创建任务时可在Web界面设置：\n\n- **计算方案**（Calculation Mode）：\n  - `cooling`（制冷）：仅计算制冷功量\n  - `heating`（制热）：仅计算制热功量\n  - `cooling+heating`（制冷+制热）：先计算制冷功量，再计算制热功量，求和得到总功量\n- **天气组选择**（Weather Group）：\n  - `china`：中国天气文件组（china_weather）\n  - `world`：世界天气文件组（world_weather）\n  - `world_weather2025`：世界2025天气文件组（world_weather2025）\n- **材料相态配置**（Material Phase Configuration）：\n  - 可配置多个温度点及其对应的发射率（Emissivity）和吸收率（Absorptivity）\n  - 每个相态点包含：\n    - 温度（°C）\n    - 发射率（0-1）\n    - 吸收率（0-1）\n- **过渡模式**（Transition Mode）：\n  - `gradient`（渐变/高斯插值）：在相邻温度点之间使用高斯插值，平滑过渡\n    - 带宽参数 σ = max(平均温度间距 × 0.4, 0.5°C)\n    - 使用高斯核函数进行加权插值\n  - `step`（突变/最近值）：使用距离当前温度最近的相态点的值\n\n### 2.2 输出结果\n\n任务完成后，在任务详情页可查看：\n\n- **柱状图**：功量对比柱状图，显示各城市/地区的制冷功量、制热功量和总功量（单位：Wh/m²）\n- **数据下载**：可下载 `data.csv` 文件，包含各城市/地区的功量数据\n\n## 3. 核心公式与物理意义\n\n### 3.1 EPW天气文件数据\n\n系统从EPW（EnergyPlus Weather）文件中读取以下逐时数据：\n\n- **DBT**（Dry Bulb Temperature）：干球温度（°C）\n- **GloHorzRad**（Global Horizontal Radiation）：水平面全球太阳辐射（W/m²）\n- **HorzIR**（Horizontal Infrared Radiation）：水平面红外天空辐射（W/m²）\n\n### 3.2 材料参数插值\n\n对于每个小时的干球温度 $T_{DBT}$，系统根据材料相态配置和过渡模式计算对应的发射率 $\\varepsilon(T_{DBT})$ 和吸收率 $\\alpha(T_{DBT})$：\n\n#### 渐变模式（高斯插值）\n\n$$\n\\varepsilon(T) = \\frac{\\sum_i \\varepsilon_i \\cdot w_i}{\\sum_i w_i}, \\quad w_i = \\exp\\left(-\\frac{(T - T_i)^2}{2\\sigma^2}\\right)\n$$\n\n$$\n\\alpha(T) = \\frac{\\sum_i \\alpha_i \\cdot w_i}{\\sum_i w_i}\n$$\n\n其中：\n- $T_i$、$\\varepsilon_i$、$\\alpha_i$ 为第 $i$ 个相态点的温度、发射率和吸收率\n- $\\sigma$ 为带宽参数：$\\sigma = \\max(\\text{平均温度间距} \\times 0.4, 0.5°C)$\n\n#### 突变模式（最近值）\n\n$$\n\\varepsilon(T) = \\varepsilon_{i^*}, \\quad \\alpha(T) = \\alpha_{i^*}\n$$\n\n其中 $i^* = \\arg\\min_i |T - T_i|$ 为距离 $T$ 最近的相态点索引。\n\n### 3.3 每小时能量计算\n\n对于每个小时：\n\n1. **总热辐射能量**（使用Stefan-Boltzmann定律）：\n\n$$\nE_{total} = \\sigma \\cdot T_K^4 \\quad [\\text{W/m²}]\n$$\n\n其中 $T_K = T_{DBT} + 273.15$ 为开尔文温度，$\\sigma = 5.670374419 \\times 10^{-8}$ W/(m²·K⁴) 为Stefan-Boltzmann常数。\n\n2. **转换为每小时能量**（Wh/m²）：\n\n$$\nE_{total,wh} = E_{total} \\times 1.0 \\quad [\\text{Wh/m²}]\n$$\n\n$$\nE_{solar,wh} = G_{solar} \\times 1.0 \\quad [\\text{Wh/m²}]\n$$\n\n$$\nE_{IR,wh} = I_{sky} \\times 1.0 \\quad [\\text{Wh/m²}]\n$$\n\n其中 $G_{solar}$ 为 Global Solar（W/m²），$I_{sky}$ 为 Infrared Sky Radiation（W/m²）。\n\n### 3.4 制冷功量计算\n\n当计算方案为 `cooling` 或 `cooling+heating` 时：\n\n1. **太阳光热吸收**：\n\n$$\nQ_{solar} = E_{solar,wh} \\times \\alpha(T_{DBT})\n$$\n\n2. **大气热发射**：\n\n$$\nQ_{atm} = (E_{total,wh} - E_{IR,wh}) \\times \\varepsilon(T_{DBT})\n$$\n\n3. **总制冷功量**：\n\n$$\nP_{cooling} = \\max(0, Q_{atm} - Q_{solar}) \\quad [\\text{Wh/m²}]\n$$\n\n如果 $P_{cooling} < 0$，则设为 0。\n\n4. **累计制冷功量**：\n\n$$\nP_{cooling,total} = \\sum_{h=1}^{8760} P_{cooling,h} \\quad [\\text{Wh/m²}]\n$$\n\n### 3.5 制热功量计算\n\n当计算方案为 `heating` 或 `cooling+heating` 时：\n\n1. **太阳光热吸收**：\n\n$$\nQ_{solar} = E_{solar,wh} \\times \\alpha(T_{DBT})\n$$\n\n2. **大气热吸收**：\n\n$$\nQ_{atm} = (E_{IR,wh} - E_{total,wh}) \\times \\varepsilon(T_{DBT})\n$$\n\n3. **总制热功量**：\n\n$$\nP_{heating} = \\max(0, Q_{solar} + Q_{atm}) \\quad [\\text{Wh/m²}]\n$$\n\n如果 $P_{heating} < 0$，则设为 0。\n\n4. **累计制热功量**：\n\n$$\nP_{heating,total} = \\sum_{h=1}^{8760} P_{heating,h} \\quad [\\text{Wh/m²}]\n$$\n\n### 3.6 总功量计算\n\n当计算方案为 `cooling+heating` 时：\n\n$$\nP_{total} = P_{cooling,total} + P_{heating,total} \\quad [\\text{Wh/m²}]\n$$\n\n### 3.7 蒸发冷却（可选：启用蒸发潜热）\n\n当勾选“启用蒸发冷却”时，在有相对湿度数据的天气文件上，会额外叠加一项**蒸发潜热制冷功量** $P_{\\text{latent}}$，只对制冷通量起增强作用。\n\n1. **基于质量传递的瞬时蒸发潜热功率**（单位：W/m²，对应代码中的 `Q_latent`）：\n\n先用 Tetens 公式计算表面与环境的饱和水汽压（单位：kPa）：\n\n$$\nP_{\\text{sat}}(T) = 0.61078 \\exp\\left( \\frac{17.27 T}{T + 237.3} \\right),\n$$\n\n其中 $T$ 为摄氏温度（°C）。设\n\n- $T_{\\text{s}}$：材料表面温度（K，代码中用干球温度近似）  \n- $T_{\\text{a}}$：环境温度（K，与 $T_{\\text{s}}$ 近似相同）  \n- $RH \\in [0,1]$：环境相对湿度\n\n则表面与环境的饱和水汽压为\n\n$$\nP_{\\text{sat,s}} = P_{\\text{sat}}(T_{\\text{s}}-273.15), \\quad\nP_{\\text{sat,a}} = P_{\\text{sat}}(T_{\\text{a}}-273.15),\n$$\n\n环境实际水汽压为\n\n$$\nP_v = RH \\, P_{\\text{sat,a}}.\n$$\n\n若 $P_{\\text{sat,s}} \\le P_v$，认为**不会发生蒸发**，取\n\n$$\nQ_{\\text{latent}} = 0.\n$$\n\n否则，将压力转换为 Pa，利用理想气体状态方程计算水汽密度：\n\n$$\n\\rho_{v,\\text{sat}} = \\frac{P_{\\text{sat,s}} \\times 1000}{R_v T_{\\text{s}}}, \\quad\n\\rho_{v,\\text{a}}   = \\frac{P_v \\times 1000}{R_v T_{\\text{a}}},\n$$\n\n其中 $R_v = 461.5\\ \\text{J/(kg·K)}$ 为水蒸气气体常数。  \n令质量传递系数 $h_m$（m/s），则蒸发质量通量为\n\n$$\n\\dot m = h_m \\bigl(\\rho_{v,\\text{sat}} - \\rho_{v,\\text{a}}\\bigr) \\quad [\\text{kg/(m²·s)}],\n$$\n\n对应的蒸发潜热功率为\n\n$$\nQ_{\\text{latent}} = \\dot m \\, L_v(T_{\\text{s}}),\n$$\n\n其中 $L_v(T_{\\text{s}})$ 为温度相关的蒸发潜热（J/kg，代码中使用经验公式在 $0$–$100^\\circ$C 范围内插值）。\n\n2. **湿润面积比例缩放与按小时积分**：\n\n考虑到真实表面通常只部分被“自由水”覆盖，引入湿润面积比例\n\n$$\n0 \\le \\text{wet\\_fraction} \\le 1,\n$$\n\n得到有效蒸发潜热功率\n\n$$\nQ_{\\text{latent,eff}} = \\text{wet\\_fraction} \\times Q_{\\text{latent}}.\n$$\n\n在功量地图中，每小时对应的**蒸发冷却能量**（Wh/m²）近似为\n\n$$\nP_{\\text{latent},h} \\approx Q_{\\text{latent,eff}} \\times 1.0 \\quad [\\text{Wh/m²}],\n$$\n\n全年累计蒸发冷却功量为\n\n$$\nP_{\\text{latent,total}} = \\sum_{h=1}^{8760} P_{\\text{latent},h} \\quad [\\text{Wh/m²}].\n$$\n\n3. **对制冷功量的修正**：\n\n在启用蒸发冷却且天气文件中含有相对湿度 `RH` 的情况下，功量地图中的**总制冷功量**实际为\n\n$$\nP_{\\text{cooling,total}}^{(\\text{with latent})}\n= P_{\\text{cooling,total}}^{(\\text{radiative})} + P_{\\text{latent,total}},\n$$\n\n其中 $P_{\\text{cooling,total}}^{(\\text{radiative})}$ 为前述 3.4–3.4.1 中仅由辐射（大气热发射与太阳吸收）贡献得到的制冷功量。\n\n## 4. 计算步骤\n\n1. **读取EPW文件**：根据选择的天气组，读取对应目录下的所有EPW文件\n2. **遍历每个EPW文件**：\n   - 读取全年8760小时的逐时数据\n   - 对每个小时：\n     - 根据干球温度 $T_{DBT}$ 和材料相态配置，计算发射率 $\\varepsilon(T_{DBT})$ 和吸收率 $\\alpha(T_{DBT})$\n     - 计算总热辐射能量 $E_{total,wh}$、太阳辐射能量 $E_{solar,wh}$ 和红外天空辐射能量 $E_{IR,wh}$\n     - 根据计算方案计算制冷功量和/或制热功量\n   - 累计该EPW文件的总功量\n3. **生成结果文件**：\n   - 生成 `data.csv` 文件，包含各城市/地区的功量数据\n   - 用于前端绘制柱状图\n\n## 5. 小案例（可复现）\n\n**目的**：计算中国各省份的制冷功量和制热功量，并绘制对比柱状图。\n\n**操作步骤**（WebUI）：\n\n1. **访问功量地图页面**：\n   - 访问\"功量地图绘制\"页面（`/power-map`）\n   - 确认当前账号为 Pro 用户（普通用户无法使用此功能）\n\n2. **配置计算参数**：\n   - **计算方案**：选择\"制冷+制热\"\n   - **天气组选择**：选择\"中国（china_weather）\"\n   - **材料相态配置**：\n     - 添加相态点1：温度 20°C，发射率 0.9，吸收率 0.6\n     - 添加相态点2：温度 30°C，发射率 0.95，吸收率 0.5\n   - **过渡模式**：选择\"渐变\"（高斯插值）\n\n3. **提交任务**：\n   - 点击\"开始计算\"按钮\n   - 系统会自动处理所有EPW文件并计算功量\n\n4. **查看结果**：\n   - 任务完成后，在任务详情页查看：\n     - **柱状图**：功量对比柱状图，显示各省份的制冷功量、制热功量和总功量（单位：Wh/m²）\n     - **数据下载**：点击\"下载 data.csv\"按钮下载完整数据\n\n**结果解读**：\n\n- **制冷功量**：表示材料在该地区全年的累计制冷能力（Wh/m²），数值越大表示制冷效果越好\n- **制热功量**：表示材料在该地区全年的累计制热能力（Wh/m²），数值越大表示制热效果越好\n- **总功量**：制冷功量与制热功量之和，用于评估材料的综合性能\n- **地区对比**：通过柱状图可以直观对比不同地区的功量差异，识别最适合材料应用的气候区域\n\n## 6. 论文写法示例\n\n> The power map calculation was performed using EPW weather files to evaluate the annual cooling and heating power of the material across different regions. Material properties (emissivity and absorptivity) were configured as temperature-dependent phase points, with Gaussian interpolation (bandwidth parameter σ = max(0.4 × average temperature spacing, 0.5°C)) used for smooth transitions between phase points. For each hour, the material properties were interpolated based on the dry bulb temperature, and the cooling power was computed as $P_{cooling} = \\max(0, (E_{total} - E_{IR}) \\times \\varepsilon - E_{solar} \\times \\alpha)$, where $E_{total} = \\sigma T_K^4$ is the total thermal radiation energy (Stefan-Boltzmann law), $E_{IR}$ is the infrared sky radiation, $E_{solar}$ is the global solar radiation, and $\\varepsilon$ and $\\alpha$ are the temperature-dependent emissivity and absorptivity, respectively. The heating power was computed as $P_{heating} = \\max(0, E_{solar} \\times \\alpha + (E_{IR} - E_{total}) \\times \\varepsilon)$. The annual power was obtained by summing the hourly power over 8760 hours. The calculation was performed for all EPW files in the selected weather group (China, World, or World 2025), and the results were visualized as bar charts comparing the power across different regions.\n"
} as any
